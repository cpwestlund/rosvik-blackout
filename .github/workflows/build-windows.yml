name: Build Windows Game

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Godot 4.6.3 and export templates
        run: |
          wget -q https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip
          wget -q https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_export_templates.tpz
          unzip -q Godot_v4.6.3-stable_linux.x86_64.zip
          chmod +x Godot_v4.6.3-stable_linux.x86_64
          mkdir -p "$HOME/.local/share/godot/export_templates/4.6.3.stable"
          mkdir -p /tmp/godot_templates
          unzip -q Godot_v4.6.3-stable_export_templates.tpz -d /tmp/godot_templates
          cp -r /tmp/godot_templates/templates/* "$HOME/.local/share/godot/export_templates/4.6.3.stable/"

      - name: Import project
        run: |
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --editor --path . --quit

      - name: Runtime smoke test
        shell: bash
        run: |
          set +e
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --path . --quit-after 12 2>&1 | tee runtime.log
          godot_status=${PIPESTATUS[0]}
          set -e
          if grep -E "SCRIPT ERROR|Parse Error|Invalid assignment|Invalid call|ERROR:" runtime.log; then
            echo "Runtime errors detected. Refusing to publish a broken build."
            exit 1
          fi
          if [ "$godot_status" -ne 0 ]; then
            echo "Godot runtime exited with status $godot_status"
            exit "$godot_status"
          fi
          for marker in ROSVIK_ART_READY ROSVIK_ANIMATIONS_READY ROSVIK_CHARACTER_PROCEDURAL_READY ROSVIK_VISUAL_FOUNDATION_READY ROSVIK_INTERIOR_CLEANUP_READY ROSVIK_SCALE_PASS_READY ROSVIK_CAMERA_PASS_READY ROSVIK_ACCURACY_02_READY ROSVIK_MULTI_CLASSROOM_READY ROSVIK_SNOW_PASSABLE_READY ROSVIK_SPORTHALL_READY ROSVIK_ICEHALL_SEPARATED_READY ROSVIK_DYNAMIC_CUTAWAY_READY ROSVIK_ARCHITECTURAL_COHESION_READY ROSVIK_DOOR_ALIGNMENT_READY ROSVIK_WALL_TOPOLOGY_READY ROSVIK_CAMERA_COHESION_READY ROSVIK_WORLD_BELIEVABILITY_04_READY ROSVIK_INTERACTIVE_DOORS_READY ROSVIK_APOCALYPSE_MOOD_READY ROSVIK_ART_REPLACEMENT_READY ROSVIK_WORLD_COHERENCE_05_READY ROSVIK_ROAD_NETWORK_READY ROSVIK_FLICKER_CLEANUP_READY ROSVIK_EXTERIOR_LINKAGE_READY ROSVIK_SPATIAL_SANITY_06_READY ROSVIK_BUILDING_MASKS_READY ROSVIK_SAFE_PLACEMENT_READY ROSVIK_CUTAWAY_REPAIR_READY ROSVIK_ROAD_AVOIDANCE_READY; do
            if ! grep -q "$marker" runtime.log; then
              echo "Missing runtime marker: $marker"
              cat runtime.log
              exit 1
            fi
          done

      - name: Export Windows build
        run: |
          mkdir -p build
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --path . --export-release "Windows Desktop" build/ROSVIK_BLACKOUT.exe

      - name: Upload playable build
        uses: actions/upload-artifact@v4
        with:
          name: ROSVIK_BLACKOUT_Windows
          path: build/ROSVIK_BLACKOUT.exe
          if-no-files-found: error
          retention-days: 30
