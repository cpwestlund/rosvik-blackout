name: Build Windows Game

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download CC0 character art
        run: |
          mkdir -p assets/character
          wget -q -O assets/character/night-striker.glb https://raw.githubusercontent.com/Seyamalam/blood-league-kickoff/aa02a4e6d8337a0604d2da131bcbbeb1f01badf0/public/assets/vendor/quaternius/night-striker.glb
          wget -q -O assets/character/universal-animation-library.glb https://raw.githubusercontent.com/Seyamalam/blood-league-kickoff/aa02a4e6d8337a0604d2da131bcbbeb1f01badf0/public/assets/vendor/quaternius/universal-animation-library.glb
          test -s assets/character/night-striker.glb
          test -s assets/character/universal-animation-library.glb

      - name: Download Godot 4.6.3 and export templates
        run: |
          wget -q https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip
          wget -q https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_export_templates.tpz
          unzip -q Godot_v4.6.3-stable_linux.x86_64.zip
          chmod +x Godot_v4.6.3-stable_linux.x86_64
          mkdir -p "$HOME/.local/share/godot/export_templates/4.6.3.stable"
          mkdir -p /tmp/godot_templates
          unzip -q Godot_v4.6.3-stable_export_templates.tpz -d /tmp/godot_templates
          cp -r /tmp/godot_templates/templates/* "$HOME/.local/share/godot/export_templates/4.6.3.stable/"

      - name: Import project
        run: |
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --editor --path . --quit

      - name: Runtime smoke test
        shell: bash
        run: |
          set +e
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --path . --quit-after 12 2>&1 | tee runtime.log
          godot_status=${PIPESTATUS[0]}
          set -e
          if grep -E "SCRIPT ERROR|Parse Error|Invalid assignment|Invalid call|ERROR:" runtime.log; then
            echo "Runtime errors detected. Refusing to publish a broken build."
            exit 1
          fi
          if [ "$godot_status" -ne 0 ]; then
            echo "Godot runtime exited with status $godot_status"
            exit "$godot_status"
          fi
          if ! grep -q "ROSVIK_ART_READY" runtime.log; then
            echo "Art/interior pass did not reach ready state."
            cat runtime.log
            exit 1
          fi
          if ! grep -q "ROSVIK_ANIMATIONS_READY" runtime.log; then
            echo "Rigged character animations did not initialize."
            cat runtime.log
            exit 1
          fi

      - name: Export Windows build
        run: |
          mkdir -p build
          ./Godot_v4.6.3-stable_linux.x86_64 --headless --path . --export-release "Windows Desktop" build/ROSVIK_BLACKOUT.exe

      - name: Upload playable build
        uses: actions/upload-artifact@v4
        with:
          name: ROSVIK_BLACKOUT_Windows
          path: build/ROSVIK_BLACKOUT.exe
          if-no-files-found: error
          retention-days: 30
